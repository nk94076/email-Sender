{
  "name": "email-sender",
  "version": "1.0.0",
  "description": "Bulk emailer with templates (text, headings, images, links, video), CSV recipient lists, and SMTP sending.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.9.1",
    "csv-parse": "^7.0.2",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^2.0.1",
    "nodemailer": "^10.0.10"
  }
}
